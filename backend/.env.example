PGHOST=localhost
PGPORT=5432
PGUSER=postgres
PGPASSWORD=postgres
PGDATABASE=restaurant_db
PGMAINTENANCE_DB=postgres
PG_SCHEMA=restaurant_app
PGPOOL_MAX=10
PG_IDLE_TIMEOUT_MS=30000
PG_CONNECT_TIMEOUT_MS=5000

# Optional for hosted PostgreSQL providers:
# DATABASE_URL=postgresql://postgres:postgres@localhost:5432/restaurant_db
# PG_SSL_MODE=require

PORT=5000
